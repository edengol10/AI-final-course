# Optional: used only by the explicitly approved public live-data workflow.
# The default committed-fixture deployment uses no secrets.
WANDB_API_KEY=

# Configure this as a GitHub Actions repository variable, not a secret.
# Leave false unless the research owner approves the restricted public profile
# (geometry/parameters + Cl/Cd; no SPOD/modal values).
PUBLIC_RESEARCH_DATA_APPROVED=false
